name: nightly

on:
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch:

jobs:
  deep-verify:
    runs-on: ubuntu-latest
    env:
      PERF_SCALE: '5000'
      FUZZ_RUNS: '500'
      FUZZ_DIFF_RUNS: '25'
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run verify
